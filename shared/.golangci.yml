version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - revive
    - gosec
    - errorlint
    - exhaustive
  settings:
    revive:
      # Explicit rule list so behavior is stable across golangci-lint versions
      # (see tools/atk-jira/.golangci.yml for context).
      rules:
        - name: unused-parameter
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/wohsj110/atlassian_cli/shared

run:
  timeout: 5m
